console.log('utils js is running')

export const square = (x) => x * x

export const add = (a, b) => a + b

const subtract = (a, b) => a - b

//export const add = (a, b) => a + b

//export {subtract as default} //default-naming is not important

export default subtract